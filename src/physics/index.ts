// import * as Render from './render';
// import * as P2 from './p2';
import * as Matter from './matter';
import * as Own from './own';

export const start = () => {
  Own.demo();
};

export const cancel = Own.cancel;